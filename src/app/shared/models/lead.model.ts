export interface Lead {
  _id: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  alternativePhoneNumber: string;
  communication: string;  
  termsAndConditions: boolean;
}