version: '3.7'

services:

  debtangel:
    container_name: debtangel
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - '4201:4200'